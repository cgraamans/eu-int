import chai from 'chai';
import chaiHttp from 'chai-http';
import 'mocha';

chai.use(chaiHttp);
const expect = chai.expect;

describe('API Login', () => {

  it('should return specified user object on call', ()=>{

    // return chai.request("http://localhost:3000").post('/v1.0/login')

  });

});